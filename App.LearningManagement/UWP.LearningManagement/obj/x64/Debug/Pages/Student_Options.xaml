<Page
    x:Class="UWP.LearningManagement.Pages.Student_Options"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP.LearningManagement.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel x:ConnectionId='2' x:Name="peopleMenu">
            <TextBox x:ConnectionId='6' x:Name="searchBox" PlaceholderText="Search for a person" VerticalAlignment="Center" HorizontalAlignment="Stretch" FontSize="14"                                    />
            <Button x:ConnectionId='7' Content="List all students" HorizontalAlignment="Stretch" FontSize="14"                         />
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:ConnectionId='18' x:Name="peopleList" HorizontalAlignment="Center" Margin="10" Visibility="Collapsed">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button x:ConnectionId='19' HorizontalAlignment="Stretch" VerticalAlignment="Top"                          >
                                <TextBlock Margin="5">
                                    <Run Text="{Binding Name}"/>
                                    <Run Text="-"/>
                                    <Run Text="{Binding Id}"/>
                                </TextBlock>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <ItemsControl x:ConnectionId='8' x:Name="studentNavigationList" HorizontalAlignment="Center" Margin="10" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center">
                    <Button x:ConnectionId='14' Content="First Page" FontSize="20"                              />
                    <Button x:ConnectionId='15' Content="Previous Page" FontSize="20"                              />
                    <TextBlock Text="{Binding CurrentPage}" FontSize="20" Margin="10"/>
                    <Button x:ConnectionId='16' Content="Next Page" FontSize="20"                              />
                    <Button x:ConnectionId='17' Content="Last Page" FontSize="20"                              />
                </StackPanel>
            </ItemsControl>
            <Button x:ConnectionId='9' Content="Provide a grade for a specific assignment" HorizontalAlignment="Stretch" FontSize="14"                              />
            <Button x:ConnectionId='10' Content="Calculate a weighted average" HorizontalAlignment="Stretch" FontSize="14"                              />
            <Button x:ConnectionId='11' Content="View current courses" HorizontalAlignment="Stretch" FontSize="14"                              />
            <Button x:ConnectionId='12' Content="View previous courses" HorizontalAlignment="Stretch" FontSize="14"                              />
            <Button x:ConnectionId='13' Content="Return to Main Menu" HorizontalAlignment="Stretch" FontSize="14"                     />
        </StackPanel>
        <Grid>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center">
                <Button x:ConnectionId='3' Content="Add a new person " FontSize="24"                        />
                <Button x:ConnectionId='4' Content=" Update a person" FontSize="24"                           />
                <Button x:ConnectionId='5' Content=" Delete a person" FontSize="24"                            />
            </StackPanel>
        </Grid>
    </Grid>
</Page>


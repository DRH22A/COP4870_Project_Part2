<Page
    x:Class="UWP.LearningManagement.Pages.Student_Options"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP.LearningManagement.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel x:ConnectionId='2' x:Name="peopleMenu">
            <TextBox x:ConnectionId='5' x:Name="searchBox" PlaceholderText="Search for a person" VerticalAlignment="Center" HorizontalAlignment="Stretch" FontSize="14"                                    />
            <Button x:ConnectionId='6' Content="List all people" HorizontalAlignment="Stretch" FontSize="14"                         />
            <ItemsControl x:ConnectionId='7' x:Name="peopleList" Margin="10" ItemsSource="{Binding Students}" Visibility="Collapsed">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" Margin="5"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button x:ConnectionId='8' Content="Provide a grade for a specific assignment" HorizontalAlignment="Stretch" FontSize="14"                              />
            <Button x:ConnectionId='9' Content="Calculate a weighted average" HorizontalAlignment="Stretch" FontSize="14"                              />
            <Button x:ConnectionId='10' Content="Return to Main Menu" HorizontalAlignment="Stretch" FontSize="14"                     />
        </StackPanel>
        <Grid>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center">
                <Button x:ConnectionId='3' Content="Add a new person " FontSize="24"                        />
                <Button x:ConnectionId='4' Content=" Update a person" FontSize="24"                           />
            </StackPanel>
        </Grid>
    </Grid>
</Page>

